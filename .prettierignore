dist
node_modules
package-lock.json
release_notes
images

# Markdown is left alone on purpose. README.md is one long option reference
# whose tables have wide description cells, and prettier pads every row to the
# width of the widest one - which makes the source unreadable and every
# documentation diff enormous.
*.md

# The MkDocs theme override is a Jinja template, not HTML: prettier reflows the
# `{% block %}` tags as if they were text.
overrides
